[package]
name = "macros"
version = "0.1.0"
edition = "2021"
autotests = false

[lib]
proc-macro = true

[[test]]
name = "tests"
path = "tests/test.rs"

[dependencies]
quote = "1.0.23"
syn = { version = "1.0.107", features = ["full", "fold"] }

[dev-dependencies]
trybuild = { version = "1.0.77", features = ["diff"] }
uefi = { path = "../uefi" }
